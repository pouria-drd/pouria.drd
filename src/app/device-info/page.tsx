import { UserDeviceInfo } from "@/components/common";

function DeviceInfoPage() {
	return (
		<main className="grow app-px flex flex-col items-center justify-center pt-8">
			<UserDeviceInfo />
		</main>
	);
}

export default DeviceInfoPage;
